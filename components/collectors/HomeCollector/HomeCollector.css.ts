import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container : {
        display : "flex",
        flexDirection : "row",
    },
    title : {
        color : "white",
        fontSize : 30,
        fontWeight : "bold",
        marginLeft : 20,
        marginBottom : -20,
    },
    wrapper : {
        padding : 10
    },
    titleContainer : {
        height : "auto"
    }
})

export default styles