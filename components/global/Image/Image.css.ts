import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container : {
        marginLeft : "auto",
        marginRight : "auto",
        padding : 10
    },
    image : {
        borderRadius : 5,
    }
})

export default styles